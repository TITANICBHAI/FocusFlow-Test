**Answer:**

Here's a tight brief you can paste directly to Replit Agent — scoped so it doesn't go wandering through the whole repo.

---

**Paste this to Replit Agent:**

> Fix two confirmed bugs and verify two suspected ones in the daily allowance feature. Do not touch unrelated files.
>
> **Fix #1 — `AppBlockerAccessibilityService.kt`:** In `usageAccumulatorRunnable.run()`, after the `accumulateTimedUsage(...)` call, also persist `timed_session_pkg` and `timed_session_open_at_ms` to SharedPrefs (same keys `onInterrupt()` already writes). Clear both keys everywhere `currentTimedPkg` is already cleared (app-switch-away block near the top of `onAccessibilityEvent`, and inside the `scheduleTimedExpiry` runnable). Goal: the restore logic in `onServiceConnected()` should be able to re-arm the expiry timer after *any* process death, not just a clean `onInterrupt()`.
>
> **Fix #2 — `AppBlockerAccessibilityService.kt`:** In the `explicitlyBlocked` branch of `onAccessibilityEvent` (the "Explicit block check" section, before the daily allowance check), if `currentTimedPkg == pkg`, cancel `timedExpireRunnable` and clear `currentTimedPkg` / `currentTimedOpenAtMs` / `currentTimedSessionEndMs` before returning.
>
> **Verify #1:** Grep the entire codebase for `setDailyAllowancePackages`. If any caller still exists outside `SharedPrefsModule.kt` itself, report it — don't remove it without asking, just tell me what calls it and from where.
>
> **Verify #2:** Find the component that renders `<DailyAllowanceModal onSave={...}>` and show me the `onSave` implementation — confirm it calls `SharedPrefsModule.setDailyAllowanceConfig(JSON.stringify(entries))` with correctly serialized data, not some other path.
>
> Don't refactor, rename, or "improve" anything else in these files. Report back what you found for the two verify items before making changes based on them.

---

**Why scoped this way:** Fix #1/#2 are things I already traced line-by-line against your actual code, so there's no ambiguity for the agent to burn tokens resolving. Verify #1/#2 are the two open leads with the highest payoff-to-cost ratio — both are single grep/read operations, not open-ended exploration. I deliberately left out the lower-priority items from my last message (BootReceiver, AppContext, other UI entry points) — those are worth a *second* pass only if the two verifies come back clean, not a first pass, since they'd cost more tokens for less certain payoff.

**Confidence:** N/A — this is a task brief, not a factual claim.

**Caveats:** If Replit Agent's report on Verify #1 or #2 turns up something, paste it back to me before it starts changing code based on those findings — a wrong assumption about what's calling `setDailyAllowancePackages` could send it down the same kind of ungrounded path we just saw from the last "diagnostic."