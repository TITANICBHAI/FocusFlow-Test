The files are already in context. Let me do the synthesis and conflict analysis directly.

---

## Synthesis: Three Proposals, One Problem

**Answer:**

All three documents propose fixes for the same root cause — `daily_allowance_used` drifting when `AppBlockerAccessibilityService` is killed mid-session. But they don't fully agree, and two of their conflicts are directly relevant to your dependency concern.

---

### Where All Three Agree (safe consensus)

- `AccessibilityService` stays as the live enforcement engine
- `UsageStatsManager` stays as the historical reporting engine
- Allowance config stays in SharedPreferences/SQLite — cannot come from Usage Access
- Add 15–30s active session checkpointing
- Fix silent bridge failures in `SharedPrefsModule.ts`
- Don't touch Digital Wellbeing
- Don't do a big-bang replacement

This is solid. If you only implement this consensus, you've already made the most impactful safe changes.

---

### Actual Conflicts Between the Three

**Conflict 1 — Who is the authority for daily open counts?**

| Doc | Position |
|---|---|
| Doc 1 | UsageEvents for *reconciliation/recovery only*, never immediate authority |
| Doc 2 | Same as Doc 1 — explicitly says this "could still allow one extra opening in an edge case" |
| Doc 3 | "Shift to UsageEvents as the **Source of Truth**" — query on every app launch |

Doc 3 is the outlier here. Doc 1 and Doc 2 both warn that UsageEvents is buffered/async and can miss the newest event immediately after a launch. Making it the primary blocking authority means you could allow one extra open that shouldn't have been allowed. **Doc 1 + Doc 2's position is correct.** UsageEvents = reconciliation after restart, not real-time blocking authority.

**Conflict 2 — The checkpoint formula (this is a concrete bug)**

Doc 3 proposes:
```
Total Usage Today = UsageStats Today + Active Session Checkpoint
```

Doc 2 explicitly flags this as wrong:
> "That can double-count if Android's UsageStats already includes part of the active session."

Doc 2's corrected version:
```
Total Usage Today = UsageStats Today + (only the portion NOT yet in UsageStats)
```

If you implement Doc 3's formula naively, usage numbers inflate. **Doc 2 is correct.** The service needs to track what's already been acknowledged by UsageStats before adding the checkpoint delta.

**Conflict 3 — How to fix bridge failures (directly touches your dependency concern)**

| Doc | Position |
|---|---|
| Doc 1 | Add a *separate* `isDailyAllowanceNativeAvailable()` — keep existing method signatures intact |
| Doc 3 | Change `setDailyAllowanceConfig` return type from `Promise<void>` → `Promise<boolean>` |

Doc 3's approach changes an existing method contract. Given your constraint ("other things depend on their current mechs"), any caller of `setDailyAllowanceConfig` that doesn't expect a boolean return could silently break. **Doc 1's approach is the safer one here** — adding a new availability check method leaves existing callers untouched.

---

### Your Dependency Constraint — What It Rules Out

"Other things depend on their current mechs" specifically protects:

- `AccessibilityService` event handling → focus mode, standalone blocks, VPN triggers, overlays all route through this. Don't restructure event flow.
- `UsageStatsModule.kt` public API shape → analytics screens already call this. Doc 1 explicitly says "preserving its API is important." Don't change return shapes.
- `daily_allowance_used` in SharedPreferences → AccessibilityService reads this for immediate enforcement today. Deleting it before the checkpoint system is validated and battle-tested breaks enforcement.
- `setDailyAllowanceConfig` signature → existing callers assume `Promise<void>`. Don't change it yet.

---

### Recommended Synthesis

Follow **Doc 1's phasing**, **Doc 2's corrections**, and use **Doc 3 only for implementation detail** — but override it on the formula and the signature change.

Concretely:

1. **Phase 1:** Add `isDailyAllowanceNativeAvailable()` as a separate check. Don't touch existing method signatures.
2. **Phase 2:** Add checkpointing every 15–30s. Keep `daily_allowance_used` intact alongside it. Use Doc 2's corrected formula: checkpoint adds only the *unreported delta*, not the full session.
3. **Phase 3:** Use UsageEvents for count reconciliation on service reconnect only — not on every app launch event.
4. **Phase 4:** Only after 2–3 weeks of validation logs showing checkpoint and UsageEvents reconciliation agree with each other — then consider reducing reliance on `daily_allowance_used`. Not before.

---

**Confidence: 0.87**
High confidence on the conflict identification — the double-counting bug and the UsageEvents authority disagreement are clearly readable from the text. Slightly lower because I'm analyzing architectural intent without seeing the actual codebase, so there may be implementation details that shift the calculus.

**Caveats:**
- The "one extra app open" edge case in UsageEvents: how bad this actually is depends on your checkpoint interval and how quickly Android flushes events — I don't have timing benchmarks for your specific device targets.
- The corrected checkpoint formula requires knowing what's "already in UsageStats" — in practice this means tracking `session_start_time` and querying UsageStats at that boundary, which is slightly more implementation work than Doc 3 implies.
- OEM behavior (Samsung, Huawei) on UsageEvents buffering can vary enough that the reconciliation window might need to be tuned per-device.