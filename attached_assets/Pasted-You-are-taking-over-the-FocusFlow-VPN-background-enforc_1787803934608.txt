You are taking over the FocusFlow VPN background-enforcement implementation.

Do not start with a broad audit or repeat the completed review. Begin implementing immediately from the state below.

Authoritative documents:
- artifacts/focusflow/VPN_BACKGROUND_ENFORCEMENT_PLAN.md
- artifacts/focusflow/VPN_PLAN_CODE_REVIEW_1787795464198.md

The recommended first-release scope is:
- Explicit VPN package selections
- Timed standalone VPN package selections
- Optional focus-to-VPN mirroring
- PER_APP routing only
- Focus mirroring defaults off
- Explicit VPN policy remains independent from Accessibility overlay policy
- Recurring schedule VPN and daily-allowance VPN mirroring remain deferred
- Do not use GLOBAL mode for focus mirroring
- Do not copy code from Silent Guardian; it is GPL-3.0

Already implemented in source:
- VpnPolicyCoordinator exists and calculates effective targets.
- Durable desired-policy state exists with version, generation, timestamps, reasons, and failed-package metadata.
- Native debounce and serialized dispatch exist.
- Stale VPN commands are rejected by policy generation.
- Explicit, timed standalone, and opt-in focus-mirror sources are supported.
- Focus mirror has an off default and basic UI.
- Recovery paths delegate through the coordinator.
- Boot watchdog rearm covers persistent always-on-only VPN configuration.
- Package add/remove/fully-remove broadcasts trigger policy recalculation.
- Expo plugin, durable installer, and manifest additions include the recovery broadcasts.
- Overlay package sources and VPN package sources are intentionally separate.
- The old JavaScript 400 ms VPN stop/start workaround has been removed.
- Basic permission, conflict, partial-failure, startup-failure, and recovery-disabled UI states exist.

Continue with source-level implementation only:

1. Finish native lifecycle ownership and reconciliation around:
   - process death
   - VPN service recreation
   - boot and user unlock
   - app replacement
   - permission revocation
   - another VPN taking control
   - standalone-to-persistent-VPN handoff
   - expired focus and standalone policies

2. Ensure every recovery path recalculates from durable policy sources at dispatch time, carries the current generation, and never trusts stale net_block_packages input as authoritative.

3. Preserve these invariants:
   - Empty PER_APP target set stops the VPN.
   - No silent fallback to GLOBAL mode.
   - Explicit VPN targets remain active even when overlay enforcement ends.
   - Ordinary overlay-only packages are not automatically treated as VPN targets.
   - Focus mirroring remains opt-in.
   - Invalid/unavailable packages remain visible as failures.
   - Another VPN is not repeatedly fought.
   - Partial registration is never reported as complete protection.

4. Complete any missing native or React Native diagnostic states needed to explain:
   - permission missing
   - another VPN active
   - service starting/rebuilding
   - partial package failure
   - startup failure
   - recovery disabled
   - desired versus applied policy generation

5. Add or update source-level contract tests for each new behavior.

6. As each item becomes genuinely complete, update:
   - artifacts/focusflow/VPN_BACKGROUND_ENFORCEMENT_PLAN.md
   - artifacts/focusflow/VPN_PLAN_CODE_REVIEW_1787795464198.md
   - artifacts/focusflow/FOCUSFLOW_TEST_PLAN.md when a test checklist item is actually covered

Do not mark device, emulator, generated Android, real-network, reboot, OEM, or lifecycle verification as complete from source inspection alone. Keep those verification items open.

Stay within the existing FocusFlow structure. Do not migrate frameworks, restructure the project, or introduce unrelated changes.