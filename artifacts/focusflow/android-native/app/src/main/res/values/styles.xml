<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- FocusFlow App Drawer Theme - Material 3 -->
    <style name="Theme.FocusFlow.AppDrawer" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/Animation.FocusFlow.BottomSheetDialog</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="bottomSheetStyle">@style/Widget.FocusFlow.BottomSheet.Modal</item>
    </style>

    <style name="Widget.FocusFlow.BottomSheet.Modal" parent="Widget.Material3.BottomSheet.Modal">
        <item name="android:background">@drawable/drawer_background</item>
        <item name="shapeAppearance">@style/ShapeAppearance.FocusFlow.CornerExtraLarge</item>
    </style>

    <!-- Bottom Sheet Dialog Animation -->
    <style name="Animation.FocusFlow.BottomSheetDialog" parent="Animation.Design.BottomSheetDialog">
        <item name="bottomSheetEnter">@anim/focus_bottom_sheet_enter</item>
        <item name="bottomSheetExit">@anim/focus_bottom_sheet_exit</item>
    </style>
</resources>