<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:layout_margin="4dp">

    <!-- Icon Frame with Badge + Allowance Indicator -->
    <FrameLayout
        android:id="@+id/app_icon_frame"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="center_horizontal">

        <!-- App Icon -->
        <ImageView
            android:id="@+id/app_icon_view"
            android:layout_width="52dp"
            android:layout_height="52dp"
            android:layout_gravity="center"
            android:scaleType="centerCrop" />

        <!-- Allowance Indicator (16dp circular progress, bottom-right) -->
        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/app_allowance_indicator"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="bottom|end"
            android:layout_margin="2dp"
            android:indeterminate="false"
            android:max="100"
            android:progress="0"
            app:indicatorColor="?attr/colorPrimary"
            app:indicatorSize="16dp"
            app:trackColor="?attr/colorSurfaceVariant"
            app:trackThickness="2dp" />

    </FrameLayout>

    <!-- App Label -->
    <TextView
        android:id="@+id/app_label_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5dp"
        android:gravity="center"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@android:color/white"
        android:textSize="11sp"
        android:textShadowColor="#CC000000"
        android:textShadowDx="0"
        android:textShadowDy="1"
        android:textShadowRadius="3" />

</LinearLayout>