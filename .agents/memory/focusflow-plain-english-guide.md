---
name: FocusFlow plain-English guide
description: Plain-English supporting context for the FocusFlow implementation plan.
---

The supporting explanation is stored at `artifacts/focusflow/BIG_PICTURE_SUPPORT.txt`. It describes the Accessibility Service as the primary guard, ForegroundTaskService as the fallback guard, and the user-visible rationale for each planned fix.

**Why:** This guide makes the architecture and priority understandable without replacing the file-level implementation plan.

**How to apply:** Use it for context and intent; use `artifacts/focusflow/FINAL_PLAN.md` for exact implementation details and sequencing.